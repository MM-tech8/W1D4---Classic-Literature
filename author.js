class Author {
    constructor (name, yearOfBirth) {
      this.name = name;
      this.yearOfBirth = yearOfBirth;
    }
}

module.exports = Author
// Maybe this form doesn't work for class.

// maybe the export default one doesn't work either ...